# UI, learning and documentation completion handoff

Current handoff, September 9, 2026. Continue the approved work; the factory specification
and `AGENTS.md` remain authoritative. **Only authenticated Claude runtime acceptance remains
blocked.** Publication and deployment are separate human-owned steps.

## Resume here

Read the [latest acceptance](acceptance/native-learning-completion-2026-09-09.md),
[actual sandbox measurements](discovery/native-sandbox-learning-2026-09-09.md), and
[interactive measurements](discovery/interactive-learning-completion-2026-09-09.md).
Inspect Git status and worktrees first. James authorized separate worktrees and parallel
agents, selected the dark operations console, and requires learning notes in Obsidian
`Project Learnings`.

The integrated branch is `feat/ui-learning-docs` at `/Users/james/factory`. Preserve the
three original untracked artifacts exactly:

- `.agents/plans/feat-runtime-certification-and-delegation/`
- `docs/runtime-frontend-acceptance-2026-09-06.md`
- `docs/runtime-nemoclaw-acceptance-2026-09-06.md`

## Completed — do not repeat without a relevant change

- All 21 fixture UI drafts and selected production dark console across seven routes;
  prior desktop/narrow, keyboard, contrast, overflow and browser checks passed.
- README/operator guidance, historical classification/archive and nine rendered workflows.
- Shared capture configuration, identity, atomic note/index, deduplication, nonblocking
  outcomes and bounded recall repairs. Native tool calls/results are now retained by parsing.
- Genuine real-vault proof: a code audit wrote
  `/Users/james/Documents/Obsidian Vault/Project Learnings/2026-09-09 factory 01a08454.md`;
  both indexes reference it and later worktree recall consumed it. Existing notes were
  unchanged. New probes used temporary vaults; no historical bulk recovery occurred.
- Codex actual interactive frontend: clean shutdown, detached note/index capture, later
  interactive recall, SIGKILL transcript survival, explicit recovery and replay. No trust
  config change occurred in these corrected probes. Automatic restart scanning is not claimed.
- Safe disposable sandbox provisioning: an empty-variable kit prevents the previously
  measured creation-time injection. Container/process checks were clean, with only the
  permitted gateway proxy entry. No global credential or production registry changed.
- Factory native transcript retention before teardown, with matched session identity,
  bounded lookup, secret quarantine, secure writes and explicit partial/unavailable fallback.
  Cancellation, suspension, orphan and review collection paths have regressions.
- Eight actual clean legacy/app-server executions plus two SIGKILL interruptions, across
  worktrees and clones including actual `sbx --clone`. Ten exports survived removal with
  matching hashes. Actual factory retain → removal → worker → note/index → later worktree
  recall passed. Both owned sandboxes were removed.
- Actual recall exposed and fixed an unrelated-note false positive from scoring the
  `Project Learnings/` namespace. The identical prompt now excludes unrelated content at
  both startup and task recall. Wider skill search still covers unindexed notes and body
  evidence alongside summary hits; automatic recall is bounded, not exhaustive.
- Shared source, all three consumer and Factory declared gates passed; independent review's
  two material findings were fixed and rechecked. The certified app-server worker is unchanged.

## Remaining: authenticated Claude capture and recall

Claude Code 2.1.266 reached the actual interactive frontend in the already-trusted factory
repository, fired Start/Prompt/End, and left a transcript. It reported expired login; the
shared detached capture returned `failed: claude exited 1`. SIGKILL also preserved its
transcript but did not fire End. Those outcomes are evidence, not authenticated success.

James was asked asynchronously to reauthenticate Claude on the host using its normal login
flow. No confirmation had arrived when this handoff was updated. Do not request secrets,
repair authentication automatically or reuse a previous error as proof of current status.
After James reports login ready:

1. Use the safe existing trusted repository context and temporary vault from the corrected
   interactive harness. Abort any trust screen before sending keys. Do not write user config.
2. Measure an actual authenticated Claude conversation, native clean shutdown and detached
   capture, note plus both indexes, replay deduplication and later relevant recall.
3. Measure interruption and explicit recovery of the owned transcript. Keep lack of an
   automatic interactive restart scanner explicit; do not run the historical backlog.
4. Archive sanitized outcomes with the actual version and unchanged trust-config hash.
   Rerun owner gates if code changes. Retire this handoff only when this acceptance passes
   or James explicitly accepts the remaining external blocker.

Earlier unsuccessful probes did persist two temporary trust entries. Exact paths remain in
[the historical record](discovery/interactive-learning-2026-09-09.md). James was informed;
removing those entries, if desired, is his action. Do not automate trust-store cleanup.

## Retention and configuration boundaries

The exporter uses the measured Codex 0.149.1/0.153.4 `state_5.sqlite` exact-session index;
unknown indices, invalid paths and files over 64 MiB produce explicit native unavailability.
`retained-native-transcript` identifies the source, not successful turn completion.
An incomplete final record is `retained-native-prefix`; partial evidence cannot replace an
existing same-session note. Keep abrupt removal before export and terminal attempts never
recollected explicit as recoverable gaps. No independent queue was added.

Process `OBSIDIAN_VAULT_DIRECTORY` is authoritative when present, including empty/invalid;
otherwise `OBSIDIAN_VAULT_DIR` is accepted. Shell-tool policy settings did not reach native
hooks in measured Codex runtimes. Use the process environment, not user config edits.
Factory export is lifecycle coordination; shared parsing, distillation and recall stay in
harness. Never run candidate worktree scripts on the host or hand-edit generated vendor.

## Worktrees and publication

Shared source `/Users/james/harness-learning-repair`, branch `fix/learning-capture-recall`,
is at `54c5c9a`, based on harness v2. Standalone consumers under `/Users/james/`, each on
`fix/learning-recall-sync`, are:

| Worktree | Head |
| --- | --- |
| python-harness-learning-repair | 0f93e93 |
| frontend-harness-learning-repair | ed20bfe |
| go-harness-learning-repair | 674b53a |

Factory feature/evidence worktrees remain: `factory-native-transcript` (`8565298`),
`factory-interactive-completion` (`e472d67`), and `factory-sandbox-learning-evidence`
(`58025a2`). Their commits are integrated; do not open overlapping duplicate factory PRs.
Earlier worktrees remain intact. Verify current heads rather than assuming this table is live.

Nothing was pushed, merged or deployed. Source lands on harness v2 first; James merges,
then consumers sync to the landed source and follow the stack/submodule publication flow.
Current vendor integrity passes; it does not establish upstream freshness. Never edit
main or the user's stack submodules. James owns live restart/deployment, credential rotation
and schema decisions. After separately approved deployment, smoke-test seven UI routes
read-only; no migration is assumed.

Run each changed repository's declared gates after further implementation. Factory requires
Ruff lint, Ruff format, mypy and pytest. Console changes additionally use the existing
[opt-in browser suite](ui-alternatives/production-validation/README.md). Check local doc links
and render changed Mermaid diagrams; preserve all earlier measured evidence.
