# Resume baseline preservation — 2026-09-07

A failed resumed invocation could persist `thread_total_wire={}` and replace an earlier valid baseline when factory next selected its adapter. Without another pre-turn runtime usage update, the worker then treated the whole thread history as current invocation usage. Pricing remained withheld, but cumulative input/output accounting was inflated.

A production-selection → production-worker regression reproduced **600 input tokens instead of the actual 100-token delta**. The test uses a real scratch Store, synthetic compatibility evidence, and fake app-server transport; it is not a fresh sandbox or provider measurement. The corrected selector ignores empty, malformed, and incomplete baseline records, preserving the latest valid counters. If no valid baseline exists, it omits the thread entry so resumed usage remains explicitly incomplete.

The retained executing protocol schema (`artifacts/crud-runtime-test/reviewer-compatibility/protocol/schema/v2/ThreadTokenUsageUpdatedNotification.json`) requires `inputTokens`, `cachedInputTokens`, `outputTokens`, `reasoningOutputTokens`, and `totalTokens`. `cacheWriteInputTokens` alone is optional with default zero. The selector follows that contract rather than treating arbitrary partial numeric dictionaries as known counters. All supplied values must be nonnegative integers, excluding booleans. A separate red regression demonstrated that input-only data had previously been accepted; the corrected selector refuses it while allowing the declared optional cache-write field to be absent.

Nine focused tests pass, covering the corrected input/output deltas, latest valid baseline preservation, missing/negative/boolean/string/noncounter/partial observations, unknown usage when no valid baseline exists, and optional cache-write compatibility. Full mypy passes 125 source files and scoped Ruff checks pass. The root updated the shared runtime-selection fixture to protocol-complete counters. Final combined baseline, runtime-workflow, and worker-compatibility validation passes 34 tests, retained in `artifacts/runtime-resume-baseline/green.txt`; the root owns the final repository gates.

Evidence: `artifacts/runtime-resume-baseline/red.txt` and `partial-red.txt`; regressions in `tests/integration/test_resume_baseline_selection.py`. Source correction is confined to `src/factory/agent/selection.py`. No original store, model, sandbox, worker source, or ticket was changed. This proves the bounded retained-baseline recovery behavior, not fresh live runtime compatibility.

Independent review of the root's worker compatibility binding found no concrete issue: selection validates current worker bytes against the manifest, and `prepare` rechecks then writes those same validated bytes, preventing an intervening host source update from launching an unmeasured worker. Its focused tests cover missing/stale hashes and source drift between selection and preparation.
