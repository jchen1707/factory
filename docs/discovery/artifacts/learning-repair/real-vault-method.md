# Real Obsidian capture and recall — September 9, 2026

This is a completed live-destination measurement, not a fixture recipe to replay.
A real read-only Codex audit read the factory transport audit and production learning,
worker and sandbox adapter sources. Its task was to assess partial evidence and the
retention seam; no synthetic lesson or verification token was supplied.

Invocation-local SessionEnd registration called the committed vendored
`codex_session_learnings.mjs` through a logger, with a three-second hook timeout.
The parent process received the existing configured vault through
`OBSIDIAN_VAULT_DIRECTORY`. The wrapper detached and the originating Codex distiller
wrote the new note after native completion. No global hook or user configuration changed.
The capture used harness a3478e0; later recall used the synced 1231a1f repair.

The initial snapshot hashed every existing Project Learnings Markdown file. Comparing
it afterward found only `_INDEX.md` changed among existing files; one new session note
was added. Both indexes reference it. No historical note was rewritten or deleted.

A later native host Codex exec ran in `/Users/james/factory-dark-console`, another
worktree of the same repository, using invocation-local SessionStart/UserPromptSubmit
hooks. Its prompt asked about the earlier retention audit without naming the note or
supplying its lesson. Tools were prohibited by the prompt. Startup context contained
one factory summary and no note body; task context contained only the new note. The
model cited the exact note and correctly described partial evidence and the proposed
retention seam. The sanitized result preserves callback source paths and response.

The operational note is `Project Learnings/2026-09-09 factory 01a08454.md`, session
`01a08454-c46b-7082-932e-02bb70b49c83`. Full runtime output remains in the normal host
session store and temporary measurement directories; it is not copied into the repo.
These measurements establish native headless host capture and real-vault recall,
not human interactive shutdown, sandbox export, permanent installation or deployment.
